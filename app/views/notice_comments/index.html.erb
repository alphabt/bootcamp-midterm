<ul>
  <% @comments.each do |comment| %>
  <li>
    <%= comment.content %>
    <%= link_to 'Edit', edit_notice_comment_path(@notice, comment) %>
    <%= link_to 'Destroy', notice_comment_path(@notice, comment),
    :method => :delete %>
  </li>
  <% end %>

</ul>

<%= link_to 'New comment', new_notice_comment_path(@notice) %>
<%= link_to 'All notices', notices_path %>
